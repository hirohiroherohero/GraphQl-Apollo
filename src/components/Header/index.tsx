import { HeaderStyle } from './style';

const Header = () => {
  return (
    <HeaderStyle>
      <h1>Movie Web</h1>
      <h2>built with React, typescript, vite, Apollo, GraphQl</h2>
    </HeaderStyle>
  );
};

export default Header;
